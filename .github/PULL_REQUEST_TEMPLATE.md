## Release Labels

- Add exactly one `release:*` label: `release:major`, `release:minor`, `release:patch`, or `release:none`
- PR title becomes the default changelog text for releases

## Notes

- `release:none` skips changelog output
- release version is chosen later during `Prepare Release`; contributors do not pick versions on PRs
